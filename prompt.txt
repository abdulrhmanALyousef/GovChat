You are a senior Flutter + Firebase engineer.

We need to implement a COMPLETE Admin Access Requests system using the SAME architecture and coding style already used in the project.

DO NOT change the existing architecture.
DO NOT introduce new patterns.
You MUST follow the current project structure and Provider state management style.

--------------------------------------------------
GOAL:

Implement Admin Access Requests feature end-to-end.

--------------------------------------------------
REQUIREMENTS:

==================================================
1) FETCH ACCESS REQUESTS (CRITICAL)
==================================================

- Fetch requests from Firestore collection:

"accessRequests"

IMPORTANT:
- Collection name is EXACTLY:
  accessRequests

--------------------------------------------------

Filter:

- organizationId == currentAdmin.organizationId
- status == "pending"

--------------------------------------------------

Display in Admin screen:
Path:

lib/roles/Admin/features/requests

--------------------------------------------------

==================================================
2) STATE MANAGEMENT (MANDATORY)
==================================================

You MUST use Provider (same style as project).

Create:

Folder:
lib/roles/Admin/features/requests/controller

Inside it:

request_controller.dart

--------------------------------------------------

Controller responsibilities:

- Fetch requests (stream or future)
- Store list of requests
- Handle loading state
- Handle approve/reject actions
- Notify listeners

--------------------------------------------------

IMPORTANT:

Follow SAME Provider pattern used in project.
Do NOT introduce Bloc, Riverpod, etc.

==================================================
3) APPROVE REQUEST LOGIC (CRITICAL)
==================================================

When admin presses "Accept":

You MUST:

1. Read request data:
   - email
   - organizationId
   - departmentId
   - name

2. Create NEW collection:

"employees"

--------------------------------------------------

Add document:

Fields:
- email
- name
- organizationId
- departmentId
- role: "employee"
- status: "active"
- createdAt

--------------------------------------------------

3. Update request:

status → "approved"

--------------------------------------------------

==================================================
4) SEND EMAIL AFTER APPROVAL
==================================================

After approval:

- Send email to employee:

"Your request has been approved. You can now log in."

--------------------------------------------------

IMPORTANT:

There is already email logic used in:

Primary Admin → Create Organization

You MUST reuse the SAME approach.

DO NOT build email system from scratch.

 NOTE:
 There is functions folder contain email send logic.
--------------------------------------------------

==================================================
5) UI UPDATE (IMPORTANT)
==================================================

After approval:

- Request should disappear from list
- List should update automatically

--------------------------------------------------

==================================================
7) DEBUG LOGS (REQUIRED)
==================================================

Add logs:

print("Admin Org ID: ...");
print("Fetched requests: ...");
print("Approved request: ...");

--------------------------------------------------

==================================================
8) TESTING (MANDATORY)
==================================================

You MUST test:

1. Create request as user
2. Login as admin
3. Confirm request appears
4. Press Accept
5. Confirm:
   ✔ Employee created in "employees"
   ✔ Request status updated
   ✔ Email sent
   ✔ Request disappears from UI

--------------------------------------------------

==================================================
OUTPUT REQUIRED
==================================================

Return:

1. request_controller.dart
2. Updated Admin Requests screen
3. Firestore logic
4. Email integration code
5. Confirmation:

"Access Requests system fully implemented and tested"

--------------------------------------------------

IMPORTANT FINAL INSTRUCTION:

- DO NOT skip Provider
- DO NOT break project structure
- DO NOT use mock data
- EVERYTHING must be connected to Firestore
- Follow the same coding style already in the project